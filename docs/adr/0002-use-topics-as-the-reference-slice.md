# Use Topics as the only executable reference slice

GReader is a team scaffold rather than a completed product. Implement Topic CRUD as the single runnable Core reference because it is small, domain-relevant, and independent of database, AI, and file-storage decisions; keep Assignment and AI as owned placeholders so their teammates retain the design and implementation work. Swagger is developer tooling and may use FastAPI's built-in JavaScript, while project-authored HTML remains script-free.
