# Handoff: OPS-14 — classroom-centric flow

Written 2026-09-24, rebuilt from the 2026-09-23 session (no handoff existed).
Read this, `AGENTS.md`, `CONTEXT.md` and `docs/adr/0007-classroom-centric-flow.md`
before touching code. Update this file before every stop.

## Where things stand

Updated 2026-09-24 (end of session).

| Branch | State |
|---|---|
| `docs/OPS-14-classroom-system` | PR #27 **merged** into `dev`. |
| `feat/OPS-14-classroom-skeleton` | PR #28 **merged** into `dev` (2026-09-24 00:38Z). |
| `feat/OPS-14-assignments-and-beyond` | Rebased on `origin/dev` 2026-09-24; local only, not pushed yet. Assignments, generation, G-00 landing; submissions in progress. |

Commits on `feat/OPS-14-assignments-and-beyond`:

- assignments: Versions, Postings, test-case kind/note, T-01 Problems/Summary and S-01 Problems/Summary filled; old CRUD endpoints removed.
- generation: Drafts, T-03 (03a/03b/03d), T-04 stepper (04a/04b), publish to several Classrooms, daily Quota; `/api/v1/generations` removed.

- G-00 landing (`cb22054`): `/` renders `shared/g00_landing.html` to visitors, logged-in accounts still redirect. Only classes already in `app.css` (no Tailwind binary locally; `static/` is outside the OPS-14 scope). Screenshots: `<scratchpad>/shots/g00-{1440,390}.png`, no horizontal scroll at 390.

Quality gate at the G-00 commit: `350 passed, 47 skipped`; ruff check and format clean.

## Decisions made after ADR-0007 (not in the ADR)

- **503 until OPS-15**: production wires `database/pending.py` (`PendingRepository`), which refuses every call; main.py maps `SliceNotPersistedError` to 503. Routers stay mounted.
- **Role rule** is "never *branch* on a role" in routes/pages; serializing one is fine. AGENTS.md wording and the architecture test changed on the feature branch.
- **Domain-field allowlists** in `tests/architecture/test_conventions.py` gained `user_id`, `instructor_id`, `classroom_id`, `student_id`, `expires_at`, `last_active_at`, `requested_at`, `joined_at`, each with the use case that reads it.
- **Verification mail** goes through a temporary auth port `VerificationMailer` (stub in `integrations/email.py`). When notifications lands, main.py points it at `NotificationService`; nothing else changes.
- **Tabs**: a Member asking for an Instructor tab gets 403; an unknown tab gets 404.
- **Demo mode** lives in `scripts/demo.py`, not a `GREADER_DEMO` switch in main.py, because fakes may not ship in `src/`. Demo card numbers are hard-coded from the prototype until assignments/submissions compute them.
- **Templates**: page templates use only the `_components/` macros for controls (architecture test). Server-rendered open modals (C-02a, C-03a) use `modal(static=true)`.
- **CSS**: Tailwind v4.3.3 standalone binary, not in the repo; README "Styles" has build/watch commands. Root font size stays 16px, body text 13px.
- `GET /classrooms/{id}/summary` lives in the assignments slice (it computes it); the CSV export waits for submissions.
- **2026-09-24 answers:** assignments and generation use the ADR shape on fakes and wire `PendingRepository` in production until OPS-15; the old SQL adapters stay unwired (the assignment adapter maps `is_hidden` to `kind` so its CI tests keep running). Submission-derived numbers come through a `PostingStats` port (seeded in demo). T-03's document list comes through a `DocumentCatalog` port (fake in demo, pending in prod).
- Generation's daily Quota is a constant 20 until A-01; `ClassroomRole` lives in `classrooms/models.py`; `AssignmentContent` in `assignments/models.py`.

## Open items

- CSRF, `Secure` cookies and the `/` redirect: done on 2026-09-24 (ADR-0007 §9.7–9.9).
- New fakes have no contract tests (no SQL adapter until OPS-15).
- `web.yaml` in the working tree is untracked and not ours; leave it.

## Next steps (in order)

1. Done: CSRF synchronizer token in the session + `csrf_field()` macro on every POST form, one shared check, 403 tests, architecture test; `Secure=True` by default, off only in `scripts/demo.py` and tests. `/` redirects to the actor's landing or `/login`; delete `home.html`.
2. Done: push `feat/OPS-14-classroom-skeleton`, rebase on `origin/dev`, open a PR to `dev` with the CI result for its head commit.
3. Done: assignments and generation (see above).
4. Next: submissions (in progress 2026-09-24) (CodeRunner stub, Run vs Submit, S-02/T-02 pages, fills `PostingStats`, summary CSV), then documents (rename `uploads`, fills `DocumentCatalog`), notifications (assignment updated/published, `EmailSender`), admin (A-01, configurable Quota), T-05 edit page.
5. Known rough edges: `/classes/{id}/assignments/{aid}` (T-02/S-02) and `/documents`, `/admin/settings` are still 404 until their slices; the demo generation client always returns the binary-search draft.

## How to check the UI

- Prototype: parse `const DATA` in `docs/wireframes/GReader wireframes - Prototype.html` with a script (`json.JSONDecoder().raw_decode`); never read it inline. The Figma MCP quota is used up.
- Screenshots: Playwright (Node) in the session scratchpad; start the demo with `GREADER_DEMO_PORT=8765 uv run python -m scripts.demo`.
